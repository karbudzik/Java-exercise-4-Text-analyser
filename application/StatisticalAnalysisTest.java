package application;

import java.util.Iterator;

import org.junit.Test;

public class StatisticalAnalysisTest {
    
    @Test
    public void should_() {
    
        // given:
        Iterator<String> fakeIterator = new FakeIterator();
        
        // when:
        StatisticalAnalysis analysis = new StatisticalAnalysis(fakeIterator);

        // then:

    } 
}